{
    "name": "sports-field-api",
    "version": "1.3.0",
    "engines": {
      "node": "16.x"
    },
    "description": "",
    "main": "app.js",
    "scripts": {
      "start": "set NODE_ENV=prod && node ./app.js",
      "watch": "set NODE_ENV=dev && nodemon --watch app ./app.js app.js",
      "dev": "set NODE_ENV=dev && nodemon --watch app ./app.js app.js",
      "release": "standard-version",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
      "body-parser": "^1.19.2",
      "cors": "^2.8.5",
      "express": "^4.17.3"
    },
    "devDependencies": {
      "nodemon": "^2.0.15"
    }
  }